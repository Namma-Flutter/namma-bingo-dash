# Bingo Game Configuration
# Note: Question count is dynamic — determined by Firebase, not set here.
ALLOW_REPETITIVE_PERSON_SCAN=false
ALLOW_SAME_USER_SCAN=false

# Firebase Configuration (required — copy from your Firebase project settings)
FIREBASE_DATABASE_URL=https://namma-bingo-d007c-default-rtdb.firebaseio.com/